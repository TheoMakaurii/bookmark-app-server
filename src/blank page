/ parent describe block
describe('Global", ()=>{ 
  // in here, attach all required hooks and connection to db

  // the following describe will group the required tests:
  describe('Tests', ()=>{
    it('test something', ()=>{
      // test something
    });

    // if you require hooks in here, you would need a context block:
    context('testing 123', ()=>{
      beforeEach('insert data',()=>{
        // insert in db;
      });
      // then continue your tests within the scope of this context, as long as these tests require the same hook/s. Otherwise, exit from its scope, and start a fresh describe
    it(' testing after hook attached', ()=>{
      // test something 
    });

});```
Chat

Stage
Theo's screen

You're sharing your screen
